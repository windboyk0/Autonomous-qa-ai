{
  "name": "@qa/fixture-admin",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "정답을 아는 목(mock) 관리자웹. 의도적 버그 10개가 심어져 있다.",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
