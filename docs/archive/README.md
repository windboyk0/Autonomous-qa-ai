# archive

여기 있는 파일은 **참고용 원본 스냅샷**이며 스펙 정본이 아니다.

| 파일 | 정체 |
|---|---|
| `autonomous-qa.md` | 블루프린트를 생성했던 Python 스크립트 + 대화 로그를 통째로 붙여넣은 파일. 내용은 `CLAUDE.md` / `agents/*.md`와 중복 |
| `electron-admin-qa-vibe-blueprint.zip` | 위 문서 17개의 zip 사본 |

스펙 정본은 저장소 루트의 `CLAUDE.md` 하나다.
에이전트 정의 정본은 `.claude/agents/` 다. (`agents/`는 Phase 0에서 이전 후 제거됨)
